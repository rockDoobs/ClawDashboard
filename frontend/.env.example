# Frontend API base URL.
# Default: empty (same-origin), so frontend calls /api on the same host.
#
# Examples:
# VITE_API_BASE_URL=
# VITE_API_BASE_URL=http://localhost:3200
# VITE_API_BASE_URL=https://your-hostname.ts.net
VITE_API_BASE_URL=
